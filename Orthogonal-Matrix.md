# Orthogonal Matrix

An orthogonal matrix is a real square matrix $`Q`$ whose transpose is equal to its inverse, meaning 
$`Q^{\top} = Q^{-1}`$ or $`Q^{\top}Q=QQ^{\top}=I`$ . The rows and columns are orthonormal vectors (perpendicular with 
a length of 1). 

[TODO]: google Orthogonal Matrix
